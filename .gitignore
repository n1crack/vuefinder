playground
/public
/node_modules
/resources
/vendor
/storage
/.idea
composer.json
composer.lock
mix-manifest.json
.env*
.DS_Store
_ide_helper.php

# Build output. Rebuilt by `npm run build`; the npm package is built and
# published by .github/workflows/release.yml, never from a committed copy.
/dist

# VitePress
docs/.vitepress/cache
docs/.vitepress/dist
